[build-system]
requires = ["setuptools>=61.0.0"]
build-backend = "setuptools.build_meta"

[project]
name = "CustomLogger"
version = "0.1.0"
dependencies = [] # Add standard library or third-party deps if needed